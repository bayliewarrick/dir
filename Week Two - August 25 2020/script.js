//we worked on last weeks assignment here, go to week one/saturday's exercises.


let array = [1, "string", 3, 45]
function findItem(itemToSearch) {
array.forEach(item => {
    if(itemToSearch == item) {
        console.log(array.indexOf(item))
    } else {
        
    }
})
};

findItem("string")